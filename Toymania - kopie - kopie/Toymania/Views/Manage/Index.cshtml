@model Toymania.Models.IndexViewModel
@{
    ViewBag.Title = "Manage";
}

<h2>Customer@*@ViewBag.Title.*@</h2>

@*<p class="text-success">@ViewBag.StatusMessage</p>*@
<div>
    @*<h4>TXT</h4>*@
    <hr />
    <dl class="dl-horizontal">
        <dt>Password:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Change your password", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Create", "SetPassword")
            }
            ]
        </dd>
        <dt>Orders:</dt>
        <dd>
            [ @Html.ActionLink("View your Orders", "Index", "Orders") ]
        </dd>
        @*<dt>History:</dt>
        <dd>
            [@Html.ActionLink("View your completed orders and order history", "Index", "Orders")]
        </dd>*@
        <dt>Cart:</dt>
        <dd>
            [ @{Html.RenderAction("CartSummary", "ShoppingCart");} ]
        </dd>
        <dt>Wishlist:</dt>
        <dd>
            [ @Html.ActionLink("View your wishlist", "Index", "WishlistManager") ]
        </dd>
        <dt>Store:</dt>
        <dd>
            [ @Html.ActionLink("View our toys store", "Index", "store") ]
        </dd>
        <dt>Update Balance:</dt>
        <dd>
            [ @Html.ActionLink("Entere a coupon code", "UB", "Coupon") ]
        </dd>
    </dl>
</div>
<h2>Admin@*@ViewBag.Title.*@</h2>
<div>
    @*<h4>TXT</h4>*@
    <hr />
    <dl class="dl-horizontal">
        <dt>Toymanager:</dt>
        <dd>
            [ @Html.ActionLink("View the Store toys in a table and add/edit/remove toys", "Index", "ToyManager") ]
        </dd>
        <dt>Stats:</dt>
        <dd>
            [ @Html.ActionLink("View sales stats for a selected year", "Index", "Stats") ]
        </dd>
        <dt>OrderDetails:</dt>
        <dd>
            [ @Html.ActionLink("View the customer orders and edit the order status", "Index", "OrderDetails") ]
        </dd>
        <dt>Usermanager:</dt>
        <dd>
            [ @Html.ActionLink("View the customer accounts and add/edit/remove accounts", "Index", "ApplicationUsers") ]
        </dd>
        <dt>Coupon Generator:</dt>
        <dd>
            [ @Html.ActionLink("Generate a Coupon", "Index", "Coupon") ]
        </dd>

        @*
        Phone Numbers can used as a second factor of verification in a two-factor authentication system.

        See
        <a href="https://go.microsoft.com/fwlink/?LinkId=403804">this article</a>
        for details on setting up this ASP.NET application to support two-factor authentication using SMS.

        Uncomment the following block after you have set up two-factor authentication
        *@
        @*
        <dt>Phone Number:</dt>
        <dd>
            @(Model.PhoneNumber ?? "None")
            @if (Model.PhoneNumber != null)
            {
            <br />
            <text>[&nbsp;&nbsp;@Html.ActionLink("Change", "AddPhoneNumber")&nbsp;&nbsp;]</text>
            using (Html.BeginForm("RemovePhoneNumber", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
            @Html.AntiForgeryToken()
            <text>[<input type="submit" value="Remove" class="btn-link" />]</text>
            }
            }
            else
            {
            <text>
                [&nbsp;&nbsp;@Html.ActionLink("Add", "AddPhoneNumber")
                }
        </dd>
        *@
        @*<dt>Two-Factor Authentication:</dt>
        <dd>
            <p>
                There are no two-factor authentication providers configured. See <a href="https://go.microsoft.com/fwlink/?LinkId=403804">this article</a>
                for details on setting up this ASP.NET application to support two-factor authentication.
            </p>*@
        @*@if (Model.TwoFactor)
        {
        using (Html.BeginForm("DisableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
        @Html.AntiForgeryToken()
        <text>
            Enabled
            <input type="submit" value="Disable" class="btn btn-link" />
        </text>
        }
        }
        else
        {
        using (Html.BeginForm("EnableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
        @Html.AntiForgeryToken()
        <text>
            Disabled
            <input type="submit" value="Enable" class="btn btn-link" />
        </text>
        }
        }*@
        </dd>
    </dl>
</div>







@*comment*@
@*<div>
    @*<h4>TXT</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Password:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Change your password", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Create", "SetPassword")
            }
            ]
        </dd>
        <dt>Orders:External Logins:</dt>
        <dd>
            @Model.Logins.Count [
                @Html.ActionLink("Manage", "ManageLogins") ]
            [@Html.ActionLink("View your current unfinished orders", "Index", "Orders")]
        </dd>
        <dt>History:</dt>
            <dd>
                [@Html.ActionLink("View your completed orders and order history", "Index", "Orders")]
            </dd>
        <dt>Cart:</dt>
        <dd>
            [@{Html.RenderAction("CartSummary", "ShoppingCart");}]
        </dd>
        <dt>Wishlist:</dt>
        <dd>
            [@Html.ActionLink("View your wishlist", "Index", "WishlistManager")]
        </dd>
        <dt>Store:</dt>
        <dd>
            [@Html.ActionLink("View our toys store", "Index", "store")]
        </dd>

        <dt>Toymanager:</dt>
        <dd>
            [@Html.ActionLink("View the Store toys in a table and add/edit/remove toys", "Index", "ToyManager")]
        </dd>
        <dt>Stats:</dt>
        <dd>
            [@Html.ActionLink("View sales stats for a selected year", "Index", "Stats")]
        </dd>
        <dt>OrderDetails:</dt>
        <dd>
            [@Html.ActionLink("View the customer orders and edit the order status", "Index", "OrderDetails")]
        </dd>
        <dt>Usermanager:</dt>
        <dd>
            [@Html.ActionLink("View the customer accounts and add/edit/remove accounts", "Index", "ApplicationUsers")]
        </dd>


        
            Phone Numbers can used as a second factor of verification in a two-factor authentication system.

            See
            <a href="https://go.microsoft.com/fwlink/?LinkId=403804">this article</a>
            for details on setting up this ASP.NET application to support two-factor authentication using SMS.

            Uncomment the following block after you have set up two-factor authentication
        
            <dt>Phone Number:</dt>
            <dd>
                @(Model.PhoneNumber ?? "None")
                @if (Model.PhoneNumber != null)
                {
                <br />
                <text>[&nbsp;&nbsp;@Html.ActionLink("Change", "AddPhoneNumber")&nbsp;&nbsp;]</text>
                using (Html.BeginForm("RemovePhoneNumber", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                @Html.AntiForgeryToken()
                <text>[<input type="submit" value="Remove" class="btn-link" />]</text>
                }
                }
                else
                {
                <text>
                    [&nbsp;&nbsp;@Html.ActionLink("Add", "AddPhoneNumber")
                    }
            </dd>
        <dt>Two-Factor Authentication:</dt>
            <dd>
                <p>
                    There are no two-factor authentication providers configured. See <a href="https://go.microsoft.com/fwlink/?LinkId=403804">this article</a>
                    for details on setting up this ASP.NET application to support two-factor authentication.
                </p>
        @if (Model.TwoFactor)
            {
            using (Html.BeginForm("DisableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
            @Html.AntiForgeryToken()
            <text>
                Enabled
                <input type="submit" value="Disable" class="btn btn-link" />
            </text>
            }
            }
            else
            {
            using (Html.BeginForm("EnableTwoFactorAuthentication", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
            @Html.AntiForgeryToken()
            <text>
                Disabled
                <input type="submit" value="Enable" class="btn btn-link" />
            </text>
            }
            }
        </dd>
    </dl>
</div>*@